steps:
  # Build and test the container image
  - name: 'gcr.io/cloud-builders/docker'
    args: [
      'build',
      '-t', 'gcr.io/$PROJECT_ID/$REPO_NAME-$_ENV-$TAG_NAME',
      '-t', '$COMMIT_SHA',
      '-f', './Dockerfile',
      '.'
    ]
  # Push the container image to Container Registry
  - name: 'gcr.io/cloud-builders/docker'
    args: ['push', 'gcr.io/$PROJECT_ID/$REPO_NAME-$_ENV-$TAG_NAME']
  # Deploy container image to Cloud Run
  - name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
    entrypoint: gcloud
    args: [
      'run',
      'deploy', '$REPO_NAME-$_ENV',
      '--image', 'gcr.io/$PROJECT_ID/$REPO_NAME-$_ENV-$TAG_NAME',
      '--region', '$_REGION',
      '--platform', 'managed',
      '--port', '3000',
      '--allow-unauthenticated'
    ]
images:
  - gcr.io/$PROJECT_ID/$REPO_NAME-$_ENV-$TAG_NAME